-- =====================================================================
-- 2026-09-11 新タイル3種の有効化（募集／イベント紹介／選挙）
--
-- 目的:
--   tile_types に is_active=false で登録しておいた3種類を、
--   script.js/admin.html側の実装が完了したので有効化する。
--   これで admin.html の「タイルを追加」に選択肢として出るようになる。
--
-- 安全性:
--   何度実行しても同じ結果になる（冪等）。既存の配置には影響しない
--   （is_active はカタログ上の選択可否を制御するだけ）。
--
-- 適用方法:
--   Supabase ダッシュボード → SQL Editor に貼り付けて実行。
-- =====================================================================

update public.tile_types
set is_active = true
where id in ('recruiting', 'event_highlight', 'election_support');
